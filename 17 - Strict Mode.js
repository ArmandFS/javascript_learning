//activate strict mode
'use strict';
//write more secure code and avoid accidental errors.

let hasDriversLicense = false;
const passTest = true;

//write logic for person that passed drivers test
//with wrong variable name on purpose
if (passTest) hasDriverLicense = true;
if (hasDriversLicense) console.log('I can drive.')

//strict mode reserve variable
const interface = 'Audio';
const private = 30;
